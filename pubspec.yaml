name: shubh_kriski_kendra
description: "A new Flutter project for Shubh Krishi Kendra."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  
  # यह 5.5.0 वर्जन आपके कोडेमैजिक बिल्ड को बिना किसी एरर के सीधे पास कराएगा
  file_picker: 5.5.0
  provider: ^6.1.2
  image_picker: ^1.0.7
  share_plus: ^7.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/data/
